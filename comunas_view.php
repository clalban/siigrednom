<?php
// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/comunas.php');
	include_once(__DIR__ . '/comunas_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('comunas');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'comunas';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`comunas`.`VcrIdCom`" => "VcrIdCom",
		"`comunas`.`VcrCom`" => "VcrCom",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => 1,
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`comunas`.`VcrIdCom`" => "VcrIdCom",
		"`comunas`.`VcrCom`" => "VcrCom",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`comunas`.`VcrIdCom`" => "CODIGO DE LA COMUNA:",
		"`comunas`.`VcrCom`" => "nombre comuna",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`comunas`.`VcrIdCom`" => "VcrIdCom",
		"`comunas`.`VcrCom`" => "VcrCom",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`comunas` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'comunas_view.php';
	$x->TableTitle = 'Comunas';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`comunas`.`VcrIdCom`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['CODIGO DE LA COMUNA:', 'nombre comuna', ];
	$x->ColFieldName = ['VcrIdCom', 'VcrCom', ];
	$x->ColNumber  = [1, 2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/comunas_templateTV.html';
	$x->SelectedTemplate = 'templates/comunas_templateTVS.html';
	$x->TemplateDV = 'templates/comunas_templateDV.html';
	$x->TemplateDVP = 'templates/comunas_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: comunas_init
	$render = true;
	if(function_exists('comunas_init')) {
		$args = [];
		$render = comunas_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: comunas_header
	$headerCode = '';
	if(function_exists('comunas_header')) {
		$args = [];
		$headerCode = comunas_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: comunas_footer
	$footerCode = '';
	if(function_exists('comunas_footer')) {
		$args = [];
		$footerCode = comunas_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
